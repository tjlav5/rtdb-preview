import { ScrollHostDirective } from './scroll-host.directive';

describe('ScrollHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHostDirective();
    expect(directive).toBeTruthy();
  });
});
