import { IfVisibleDirective } from './if-visible.directive';

describe('IfVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new IfVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
